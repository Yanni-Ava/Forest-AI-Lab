# 2026-09-02 负责人工作记录

负责人：王思丹  
模块：AI算法 + Web系统 + 项目管理

## 今日完成

### 1. AI Alpha Demo

完成图片识别 Demo 的 Alpha 升级：

- 后端接口继续支持图片上传识别。
- 接口新增植被覆盖率计算。
- 接口新增植被叠加图输出。
- 前端页面新增植被覆盖率展示。
- 前端页面新增当前模型展示。

验证结果：

- API 返回状态：200
- 示例图片植被覆盖率：49.96%
- 前端构建：通过

### 2. 公开森林图片数据集

建立 `vegetation_v2_public`：

- 来源：Wikimedia Commons 公开森林图片。
- 数量：18 张。
- 划分：train 12、val 3、test 3。
- 标签：ExG/Otsu 伪标注。
- 数据集检查：通过。

### 3. Public Alpha 模型训练

完成实验：

```text
EXP-20260902-PUBLIC-TREE-SEG-ALPHA
```

结论：训练流程完成，但测试指标偏低，暂不替换 Demo 主模型。

### 4. 伪标注质量检查

生成总览图：

```text
docs/evidence/0902_annotation_review/vegetation_v2_public_label_contact_sheet.jpg
```

筛选结果：

- 12 张可优先人工修标。
- 6 张因雾、天空或浅色背景误标严重，暂缓使用。

### 5. Review Subset 模型训练

建立 `vegetation_v2_public_review_subset` 并完成训练：

```text
EXP-20260902-REVIEW-SUBSET-TREE-SEG-ALPHA
```

测试结果：

- Mask mAP50：约 0.165
- 比 Public Alpha 全量伪标注模型有提升
- 但仍不足以替换当前 Demo 主模型

## 当前模型选择

当前 Demo 主模型继续使用：

```text
EXP-20260902-BOOTSTRAP-TREE-SEG-V1
```

原因：展示更稳定，已完成 API 接入验证。

Public Alpha 和 Review Subset Alpha 作为模型优化实验记录保留。

### 6. Auto Refined Alpha V0.2

完成自动修标实验：

```text
EXP-20260902-AUTO-REFINED-TREE-SEG-ALPHA-V02
```

结果：

- 自动修标可视化更干净。
- 测试 Mask mAP50 约 0.0058。
- 指标没有超过 Review Subset 模型。

结论：自动修标不能替代人工修标，下一步仍应做人工精标。

## 今日结论

9 月负责人任务已经从“能跑 Demo”推进到“有数据集版本、有训练实验、有模型对比、有标注质量检查、有 Web 联调”的 Alpha 阶段。

下一步不是盲目继续训练，而是进行人工修标，然后训练 Alpha V0.2。
